############################################
# Foundry VTT Module (.gitignore)
# PF2e Macro Pack + LevelDB Compendiums
############################################

############################################
# OS / Editor Junk
############################################

.DS_Store
Thumbs.db
desktop.ini

.vscode/
.idea/

############################################
# Foundry Compendium Packs (LevelDB)
#
# Foundry v12+ uses LevelDB folders for packs.
# These files are generated and MUST NOT be
# committed to Git or they will break.
#
# Only commit pack.json metadata.
############################################

# LevelDB internals
packs/**/CURRENT
packs/**/LOCK
packs/**/LOG
packs/**/LOG.old
packs/**/MANIFEST-*

# Any log/database fragments
packs/**/*.log
packs/**/*.ldb
packs/**/*.db

############################################
# Allow pack metadata files (commit these)
############################################

!packs/**/pack.json

############################################
# Release Artifacts (never commit zips)
############################################

*.zip
*.tar.gz

############################################
# Node / Build Tooling (if added later)
############################################

node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

############################################
# Optional: Ignore Foundry cache/temp files
############################################

*.tmp
*.bak
